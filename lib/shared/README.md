# shared

複数 feature で共有する共通要素。

- `constants`: 共通キー・固定値
- `dto`: 汎用転送モデル
- `enums`: 共通 enum
- `errors`: 共通エラー型
- `extensions`: 共通 extension
- `services`: アプリ全体で使う基盤 service
- `theme`: テーマとデザイントークン
- `utils`: 共通 helper / formatter
- `widgets`: 再利用 UI コンポーネント
